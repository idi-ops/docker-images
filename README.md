# docker-node-browsers

Fedora 28 image with:

* Firefox
* Chrome
* Node.js
* npm packages:
  * grunt-cli
  * istanbum
  * testem

# Running:

```bash
$ docker run --cap-add=SYS_ADMIN inclusivedesign/node-browsers  
``s`
